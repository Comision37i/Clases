/* let numeros = [1, 5, 10, 15];
let dobles = numeros.map(function(x) {
    return x * 2;
});
console.log(dobles);
console.log(numeros); */

/* let numeros = [1, 4, 9];
let raices = numeros.map(function(num){
    return Math.sqrt(num);
})
let dobles = raices.map(function(numero){
    return numero * 2;
})


console.log(numeros);
console.log(raices);
console.log(dobles);
console.log(numeros);
console.log(raices); */

/* let itemsNavbar = ['Inicio', 'Sobre Nosotros', 'Noticias', 'Contacto', 'Productos'];
let printItems = itemsNavbar.map(function(item){
   return  item.toUpperCase();
});

console.log(printItems)
console.log(itemsNavbar) */
/* const palabras = ['hola', 'chau', 'manzana', 'Chevrolet']
palabras.map(function(palabra){
    console.log(palabra.slice(0, 1))
}) */